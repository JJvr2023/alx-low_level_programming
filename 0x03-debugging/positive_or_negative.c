#include "main.h"
/**
 * main - Determine if a number is positive, negative or zero
 * 0: is number to be checked
 * Return: 0
 */

void positive_or_negative(int i)
{
	if (i < )
	{
	printf("%d is %ds/n", "negative");
	}
	else if (i > 0)
	{
	printf("%d is %ds/n", "positive");
	}
	else
	{
	printf("%d is %ds/n", "zero");
	}
	return;
}
