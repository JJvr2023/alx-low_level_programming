#include<stdio.h>

/**
 * main - Main entry point.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n = 97;
	int m = 65;

	while (m <= 90)
	{
		putchar(m);
	}
	while (n <= 122)
	{
		putchar(n);
	}
	putchar('/n');
	return (0);
}
